{
  "name": "kadosh",
  "version": "1.0.0",
  "description": "POS e inventario para pequeños negocios",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:db": "cd db && ./pocketbase serve",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:db\"",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "concurrently": "10.0.3"
  }
}